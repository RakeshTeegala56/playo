Git-Commands
-------------
```sh
# cloning the repo
git clone <repo-ssh-url>

# initialize the repo
git init

# pulling the changes
git pull


git status
git add .
git commit -m 'message'
git push -u origin main

```
![alt text](images/git.png).